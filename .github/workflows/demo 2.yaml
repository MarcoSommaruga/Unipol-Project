name: GitHub Actions Demo 2
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: linux-latest
    steps:
      - run: echo "Ho attivato questo nuovo job!"
